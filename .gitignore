# Binaries
/striatum
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test and coverage
*.test
coverage.out
coverage.html

# Striatum pack output: oci.Pack writes <project>/build/ for any project root (including
# e.g. packages/foo/build/). Root-only /build would miss those. If you must commit a
# directory named build/, use git add -f or a negated pattern under that path.
build/

# User cache / config (pull, skill install)
.striatum/

# IDE and editor
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Go workspace
go.work
go.work.sum
